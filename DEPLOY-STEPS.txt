DEPLOYMENT STEPS:

1. CREATE GITHUB REPO (browser opened):
   - Repository name: mentorship-dashboard
   - Make it public
   - Don't initialize with README
   - Click "Create repository"

2. PUSH CODE TO GITHUB:
   Run these commands in terminal:
   
   git remote add origin https://github.com/YOUR_USERNAME/mentorship-dashboard.git
   git branch -M main
   git push -u origin main

3. DEPLOY ON RENDER (browser opened):
   - Sign up/login with GitHub
   - Click "New +" → "Web Service"
   - Connect your mentorship-dashboard repo
   - Render auto-detects render.yaml
   - Click "Create Web Service"

4. SET ENVIRONMENT VARIABLES in Render:
   - EMAIL: your-gmail@gmail.com
   - APP_PASS: your-gmail-app-password

5. DONE - Your app will be live at: https://your-app-name.onrender.com